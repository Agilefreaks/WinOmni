<UserControl x:Class="OmniHolidays.MessagesWorkspace.ContactList.ContactListContent.Busy"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:properties="clr-namespace:OmniHolidays.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="{x:Static properties:Resources.FetchingContacts}" HorizontalAlignment="Center" FontFamily="{DynamicResource ActivityContentFont}" 
                   FontSize="{DynamicResource ActivityContentFontSize}" Foreground="{DynamicResource ActivityContentBrush}"
                   TextWrapping="Wrap" TextAlignment="Center"/>
        <controls:MetroProgressBar IsIndeterminate="True" Minimum="0" Maximum="100" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </StackPanel>
</UserControl>
