<UserControl x:Class="Omnipaste.Clipping.ClippingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Width="50" Height="50" Margin="6" IsEnabled="False" Style="{DynamicResource MetroCircleButtonStyle}" Visibility="{Binding Path=IsCloudVisible, Converter={StaticResource BooleanToVisibility}}">
            <Rectangle Width="22" Height="22" Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_cloud}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>

        <Button Width="50" Height="50" Margin="6" IsEnabled="False" Style="{DynamicResource MetroCircleButtonStyle}" Visibility="{Binding Path=IsLaptopVisible, Converter={StaticResource BooleanToVisibility}}">
            <Rectangle Width="22" Height="22" Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                <Rectangle.OpacityMask>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_laptop}" />
                </Rectangle.OpacityMask>
            </Rectangle>
        </Button>

        <TextBlock Grid.Column="1" Text="{Binding Path=Model.Content}" TextWrapping="Wrap" HorizontalAlignment="Stretch"/>
    </Grid>
</UserControl>