<UserControl x:Class="Omnipaste.Notification.HyperlinkNotification.HyperlinkNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:Omnipaste.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button Style="{DynamicResource HyperlinkButtonStyle}" ToolTip="{x:Static p:Resources.ButtonBrowse}" x:Name="OpenLink">
            <TextBlock Style="{DynamicResource NotificationHyperlinkStyle}">
                <Hyperlink NavigateUri="{Binding Path=Uri, Mode=OneWay}">
                    <Run Text="{Binding Path=Uri, Mode=OneWay}" />
                </Hyperlink>
            </TextBlock>
        </Button>
    </Grid>
</UserControl>
