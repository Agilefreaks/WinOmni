<UserControl x:Class="Omnipaste.Notification.IncomingCallNotification.IncomingCallNotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:Omnipaste.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Content="{x:Static p:Resources.IncommingCallNotificationSendSms}" Style="{DynamicResource NotificationPrimaryButtonStyle}" x:Name="ReplyWithSMS"/>
        <Button Grid.Column="1" Content="{Binding Path=EndCallButtonText}" Style="{DynamicResource NotificationSecondaryButtonStyle}" x:Name="EndCall" />
    </Grid>
</UserControl>