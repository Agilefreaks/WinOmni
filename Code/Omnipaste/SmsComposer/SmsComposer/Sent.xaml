<UserControl x:Class="Omnipaste.SmsComposer.SmsComposer.Sent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:p="clr-namespace:Omnipaste.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Content="{x:Static p:Resources.SmsComposerSuccess}" Style="{StaticResource DescriptionHeaderStyle}" />
        <StackPanel Grid.Row="1" Style="{StaticResource ButtonsPanel}">
            <Button x:Name="TryClose" Content="{x:Static p:Resources.ButtonOk}" IsCancel="True"  Style="{StaticResource PrimaryDialogButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
