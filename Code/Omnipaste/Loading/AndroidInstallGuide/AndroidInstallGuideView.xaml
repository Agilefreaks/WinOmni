<UserControl x:Class="Omnipaste.Loading.AndroidInstallGuide.AndroidInstallGuideView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <Label Style="{StaticResource HeaderStyle}">Great! Omnipaste installed successfully.</Label>
            <Label Style="{StaticResource DescriptionHeaderStyle}">Now, get Omnipaste for your other devices.</Label>
            <Label Style="{StaticResource HeaderStyle}">We've sent you an installation email.</Label>
            <Label Style="{StaticResource HeaderStyle}">Open the email from the devices you want and follow instructions.</Label>
        </StackPanel>

        <Button Grid.Row="1" Content="Okay, got it" IsDefault="True" x:Name="Ok" Style="{StaticResource PrimaryDialogButton}" />
    </Grid>
</UserControl>
