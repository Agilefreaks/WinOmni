<UserControl x:Class="Omnipaste.MasterEventList.Calling.CallingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Label Content="Your phone will start calling soon. Please pick it up." Style="{StaticResource DescriptionHeaderStyle}" />
        
        <controls:MetroProgressBar Grid.Row="1" IsIndeterminate="True"
                                Minimum="0" Maximum="100" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        
        <StackPanel Grid.Row="2" Style="{StaticResource ButtonsPanel}">
            <Button x:Name="TryClose" Content="Ok" IsDefault="True" IsCancel="True" Style="{StaticResource PrimaryDialogButton}" />
        </StackPanel>
    </Grid>
</UserControl>
