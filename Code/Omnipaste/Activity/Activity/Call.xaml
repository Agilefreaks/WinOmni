<UserControl x:Class="Omnipaste.Activity.Activity.Call"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:globalization="clr-namespace:System.Globalization;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="23"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="37"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" x:Name="Model_Time" Text="{Binding Path=Model.Time, Converter={StaticResource HumanizedStringConverter}, 
            ConverterCulture={x:Static globalization:CultureInfo.CurrentCulture}}"
                   Style="{StaticResource ActivityTimeStampStyle}"/>
        <Border Grid.Row="0" Grid.Column="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Grid.ColumnSpan="3"  Style="{DynamicResource ActivityContentBorder}" BorderBrush="{DynamicResource CallBrush}">
                    <TextBlock x:Name="Model_Content" Style="{DynamicResource ActivityContentTextBlockStyle}"/>
                </Border>
                <Ellipse Grid.Column="0" Style="{DynamicResource ActivityIconBorder}"/>
                <Rectangle Grid.Column="0" Style="{DynamicResource ActivityIcon}" Fill="{DynamicResource CallBrush}"/>
            </Grid>
        </Border>
        <TextBlock x:Name="Model_Device" Grid.Row="1" Grid.Column="1" Style="{DynamicResource ActivityDeviceNameStyle}"/>
    </Grid>
</UserControl>
