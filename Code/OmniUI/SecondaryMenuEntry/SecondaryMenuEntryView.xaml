<UserControl x:Class="OmniUI.SecondaryMenuEntry.SecondaryMenuEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org">

    <Button Visibility="{Binding Path=CanPerformAction, Converter={StaticResource BooleanToVisibilityConverter}}" cal:Message.Attach="PerformAction()" Style="{DynamicResource SideSecondaryButtonStyle}" ToolTip="{Binding Path=ToolTipText}" Content="{Binding Path=Icon, Converter={StaticResource StringToResourceConverter}}"/>
    
</UserControl>
