<UserControl x:Class="OmniUI.HeaderButton.HeaderItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:OmniUI.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:StringToResourceConverter x:Key="StringToVisualBrushConverter"/>
    </UserControl.Resources>
    <Grid>
        <Rectangle Width="22" Height="22" Opacity="0.5" Margin="9,1,9,1" Fill="{DynamicResource IdealForegroundColorBrush}" ToolTip="{Binding Path=ButtonToolTip}">
            <Rectangle.OpacityMask>
                <VisualBrush Stretch="Uniform" Visual="{Binding Path=Icon, Converter={StaticResource StringToVisualBrushConverter}}" />
            </Rectangle.OpacityMask>
        </Rectangle>
        <Rectangle SnapsToDevicePixels="True"
                   IsHitTestVisible="False"
                   Opacity="0.25"
                   Width="1"
                   Height="15"
                   VerticalAlignment="Stretch"
                   HorizontalAlignment="Right"
                   Fill="{DynamicResource IdealForegroundColorBrush}"
                   Margin="8,0,0,0" />
    </Grid>
</UserControl>
