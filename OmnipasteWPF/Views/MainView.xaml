<Window x:Class="OmnipasteWPF.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Cinch="clr-namespace:Cinch;assembly=Cinch"
        xmlns:taskbarNotification="clr-namespace:Hardcodet.Wpf.TaskbarNotification;assembly=Hardcodet.Wpf.TaskbarNotification"
        Title="Omnipaste" Height="0" Width="0" Icon="/OmnipasteWPF;component/Resources/Icon2.ico" ShowInTaskbar="False" 
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="NoResize"
        Loaded="OnLoaded" Cinch:LifetimeEvent.Loaded="{Binding Path=LoadedCommand}">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <taskbarNotification:TaskbarIcon DataContext="{Binding Path=TrayIconViewModel}" 
                                         Visibility="{Binding Path=TrayIconVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                                         ToolTipText="test"
                                         IconSource="/OmnipasteWPF;component/Resources/Icon2.ico"/>
    </Grid>
</Window>
